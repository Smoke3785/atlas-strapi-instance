import styled from "styled-components";
import { Box } from "@strapi/design-system";

const atlasInputAttributes = {
  className: "atlasInputRoot",
};

const AtlasInputRoot = styled(Box).attrs(atlasInputAttributes)``;

export default AtlasInputRoot;
export { AtlasInputRoot };
